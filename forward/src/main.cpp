#include <iostream>

int main(int argc, char* argv[]) {
    // TODO: parse command-line arguments
    // forward.cpp reads preprocessed data + trial params
    // writes raw misfits (unweighted, per-module)
    std::cout << "fm_forward: GPU misfit computation (placeholder)" << std::endl;
    return 0;
}