# Handoff — Focal Mechanism Inversion (CUDA Rewrite)

**Date**: 2025-06-23
**Project**: `/home/guochang/Projects/refactor-fm`
**Last commits** (most recent first):

| Commit    | Message                                                     |
|-----------|-------------------------------------------------------------|
| `d61809d` | docs: update docs and tests for new file layout             |
| `cac55a8` | refactor: remove old stage dirs and shared packages         |
| `f279626` | feat: add flat stage scripts                                |
| `7eb5959` | feat: add shared packages (io, mt, grid, signal, aggregate) |

## Current State

### File layout (final)

```
scripts/                    ← Flat stage scripts (no main(), zero function defs)
  input.jl, preprocess.jl, assess.jl, output.jl
shared/                     ← Julia packages by function
  io/        (module: IO)       ← HDF5 I/O + geophysics utils
  mt/        (module: MT)       ← SDR ↔ MT conversion
  grid/      (module: Grid)     ← Trial generation + grid refinement
  signal/    (module: Signal)   ← Waveform preprocessing
  aggregate/ (module: Aggregate)← Misfit aggregation + uncertainty helpers
forward/                    ← C++ CMake project (unchanged)
driver.sh                   ← Updated paths to scripts/
tests/                      ← Updated references
```

All 4 scripts compile cleanly:

```
julia --compile=min -e 'include("scripts/input.jl")'
julia --compile=min -e 'include("scripts/preprocess.jl")'
julia --compile=min -e 'include("scripts/assess.jl")'
julia --compile=min -e 'include("scripts/output.jl")'
```

### Script flattening (this session)

- Removed all `main()` wrappers from `scripts/input.jl`, `scripts/assess.jl`, `scripts/output.jl`
- `scripts/preprocess.jl` was already flat
- Zero `function` definitions in any script (verified with `grep -c 'function ' scripts/*.jl`)
- Straight-line top-level execution — scripts load shared modules via `include()` + `using .ModuleName`, then run inline

### Tooling moved to shared/ (this session)

| Function              | Moved from          | Moved to                            |
|-----------------------|---------------------|-------------------------------------|
| `parse_time_iso`      | `scripts/input.jl`  | `shared/io/src/IO.jl`               |
| `haversine_distance`  | `scripts/input.jl`  | `shared/io/src/IO.jl`               |
| `compute_azimuth`     | `scripts/input.jl`  | `shared/io/src/IO.jl`               |
| `extract_station`     | `scripts/input.jl`  | `shared/io/src/IO.jl`               |
| `extract_phase_type`  | `scripts/input.jl`  | `shared/io/src/IO.jl`               |
| `find_latest_status`  | `scripts/output.jl` | `shared/io/src/IO.jl`               |
| `compute_depth_range` | `scripts/output.jl` | `shared/aggregate/src/Aggregate.jl` |
| `compute_sdr_std`     | `scripts/output.jl` | `shared/aggregate/src/Aggregate.jl` |

### Docs updated (this session)

- `doc/design.md` — added project layout tree
- `doc/stages/input.md`, `doc/stages/assess.md`, `doc/stages/output.md` — added Script Style sections documenting flat + shared-tooling pattern
- `doc/modules/hdf5_io.md` — listed new utility exports
- `doc/modules/aggregator.md` — added uncertainty helpers
- Deleted `doc/handoff-2025-06-22.md`, `doc/session-summary-2025-06-22.md`

## Uncommitted Changes

10 files modified — docs updates + flattening + tooling moves. Ready to commit as:

1. **Commit**: "refactor: flatten scripts and move tooling to shared/"
   - `scripts/*.jl` — remove main(), zero functions, straight-line
   - `shared/io/src/IO.jl` — add 6 utility functions
   - `shared/aggregate/src/Aggregate.jl` — add 2 uncertainty functions
1. **Commit**: "docs: document flat scripts and shared tooling"
   - `doc/modules/aggregator.md`, `doc/modules/hdf5_io.md`
   - `doc/stages/assess.md`, `doc/stages/input.md`, `doc/stages/output.md`

## Suggested Skills

- `verification-before-completion` — before claiming work done, run tests
- `executing-plans` or `subagent-driven-development` — for multi-step implementation
- `tdd` — if adding new test coverage
- `systematic-debugging` — if tests fail
- `caveman` — if token efficiency is needed for large diffs

## Next Steps (suggested)

1. Commit uncommitted changes in 2 logical groups (see above)
1. Run `tests/test_e2e.sh` to validate the full pipeline with the new flat scripts
1. Compile C++ forward binary and run `tests/run_cross_lang.sh`
1. Update `shared/aggregate/Project.toml` with correct peer UUIDs if needed
1. Regenerate `Manifest.toml` files for shared packages
1. Consider adding unit tests for the new utility functions in `shared/io/` and `shared/aggregate/`

## Reference Documents

- Project layout and design: `doc/design.md`
- HDF5 schemas: `doc/schema.md`
- Stage responsibilities: `doc/stages/input.md`, `doc/stages/preprocess.md`, `doc/stages/assess.md`, `doc/stages/output.md`, `doc/stages/forward.md`, `doc/stages/driver.md`
- Module specifications: `doc/modules/hdf5_io.md`, `doc/modules/mt_utils.md`, `doc/modules/trial_gen.md`, `doc/modules/waveform_proc.md`, `doc/modules/aggregator.md`, `doc/modules/solution_comp.md`, `doc/modules/convergence.md`
- Project instructions: `AGENTS.md`
