# Handoff — Focal Mechanism Inversion (CUDA Rewrite)

**Commit:** `da6720b` (HEAD)  
**Branch:** `master`  
**Status:** Working tree clean. 421 tests pass across all 7 suites.

## What was done

This session finished extracting two reusable modules from stage-local directories into `shared/` independent packages, eliminating a naming collision between `input/src/preprocess.jl` (DSP) and `preprocess/src/preprocess.jl` (stage entry point):

| Module | Old location | New location |
|--------|-------------|--------------|
| `WaveformPreprocessing` | `input/src/preprocess.jl` | `shared/WaveformPreprocessing.jl/src/WaveformPreprocessing.jl` |
| `TrialGen` | `preprocess/src/trial_gen.jl` | `shared/TrialGen.jl/src/TrialGen.jl` |

Five consumer files updated to point to new shared paths. All tests pass.

The previous session (commits `6ded2ec`–`c31792b`) resolved 7 doc-code consistency issues, cleaned up finished tasks, cancelled permanently deferred features (CAP, FFT-based XCorr, Batch), and removed stale `.h5` artifacts and `doc/plan.md`.

Full session history: `doc/session-summary-2025-06-22.md`

## Suggested skills

- **verification-before-completion** — before committing any changes
- **executing-plans** — if implementing any of the remaining tasks below
- **systematic-debugging** — for the E2E test bug and driver.sh path issues
- **caveman** — if the user wants compact communication

## Key project references

| Doc | Path |
|-----|------|
| Pipeline architecture | `doc/design.md` |
| HDF5 schemas | `doc/schema.md` |
| Per-stage docs | `doc/stages/{input,preprocess,forward,assess,output}.md` |
| Per-module docs | `doc/modules/{hdf5_io,solution_comp,waveform_proc,data_cache}.md` |
| Project AGENTS | `AGENTS.md` |
| Handoff | `HANDOFF.md` |
| Session summary | `doc/session-summary-2025-06-22.md` |

## Pending issues

1. **E2E test bug** — `tests/test_e2e.sh` step 8 expects `status_2.h5` on `break` but `assess.jl` marks `converged=1` without creating new file
2. **driver.sh path** — binary at `forward/build/forward`, script references `build/forward/forward`
3. **CUDA backend** — untested, needs `spack load cuda; cmake -DBACKEND=CUDA ..; make`
4. **Real data ingestion** — pipeline only tested with synthetic data
5. **Resume-after-interruption** — incremental mode not tested
